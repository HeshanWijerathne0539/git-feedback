name: Ex2

on:
  push:
    branches: [main]

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: print
        run: echo "Hello World"
      - name: print-test
        run: echo "This is a test workflow"